HIDE NAME,PHONE,SEM,SECTION,DEPT

ON USN ENTERED
	IF USN EXISTS IN STUDENTS
		FILL NAME,PHONE,SEM,SECTION,DEPT
	ELSE SHOW NAME,PHONE,SEM,SECTION,DEPT

ON REGISTER CLICKED
	IF USN NOT EXISTS IN STUDENTS
			ADD TO STUDENT USN, NAME,PHONE,SEM,SECTION,DEPT
	IF USN, E_ID PAIR NOT EXISTS IN REGISTRATION
		ADD TO REGISTRATION E_ID, USN, DESK_USN
		# TRIGGER WILL ADD TRANSACTION AS PENDING
	ELSE SHOW ERROR DUPLICATE REGISTRATION
	RID IN QR CODE

	UPDATE TRANSACTIONS SET AMOUNT=AMOUNT, MODE=PAYMENTMODE STATUS=PAID WHERE RID=RID

	GENERATE QR CODE
	DISPLAY QR CODE

	SEND THE M-TICKET IN STUDENT PHONE

